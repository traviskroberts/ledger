<!DOCTYPE html>
<html>
<head>
  <title>Ledger</title>
  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
  <%= stylesheet_link_tag 'application', :media => 'all' %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div class="body-wrap">
    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>

          <%= link_to 'Ledger', root_path, :class => 'brand' %>

          <div class="nav-collapse collapse">
            <%- if current_user -%>
              <ul class="nav pull-right">
                <li class="<%= current_page?(my_account_path) ? 'active' : '' %>"><%= link_to "Logged in as #{current_user.email}", my_account_path %></li>
                <li class="divider-vertical"></li>
                <%- if current_user.super_admin? -%>
                  <li class="<%= controller.controller_name == 'users' ? 'active' : '' %>"><%= link_to 'Users', users_path %></li>
                  <li class="divider-vertical"></li>
                <%- end -%>
                <li><%= link_to 'Logout', logout_path, :method => :delete %></li>
              </ul>
            <%- else -%>
              <ul class="nav pull-right">
                <li class="<%= current_page?(login_path) ? 'active' : '' %>"><%= link_to 'Login', login_path %></li>
                <li class="divider-vertical"></li>
                <li class="<%= current_page?(register_path) ? 'active' : '' %>"><%= link_to 'Register', register_path %></li>
              </ul>
            <%- end -%>

            <ul class="nav">
              <%- if current_user -%>
                <li class="<%= controller.controller_name == 'accounts' ? 'active' : '' %>"><%= link_to 'Accounts', accounts_path %></li>
              <%- end -%>
            </ul>
          </div> <!--/nav-collapse -->
        </div> <!-- /container -->
      </div> <!-- /navbar-inner -->
    </div> <!-- /navbar -->

    <div class="container main clearfix">
      <%= render 'shared/flash' %>
      <%= yield %>
    </div> <!-- /container -->

    <div class="footer-push"></div>
  </div> <!-- /body-wrap -->

  <footer>
    <div class="container">
      <!-- Footer? -->
    </div>
  </footer>

  <!-- <p id="screen-info"></p> -->

  <%= javascript_include_tag 'application' %>
</body>
</html>
