<h1>Update Recurring Transaction</h1>

<%= form_for @recurring_transaction, :url => account_recurring_transaction_path(@account, @recurring_transaction), :html => {:class => 'form-horizontal'} do |f| %>
  <fieldset>
    <%= render 'form', :f => f %>

    <div class="control-group">
      <div class="controls">
        <%= f.submit 'Update', :class => 'btn btn-primary' %>
        <%= link_to 'Cancel', account_recurring_transactions_path(@account), :class => 'btn' %>
      </div>
    </div>
  </fieldset>
<%- end -%>
