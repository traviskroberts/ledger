<% flash().each do |key, msg| %>
  <div class="alert alert-fixed alert-<%= key %> fade in">
    <%= link_to "&times;".html_safe, "#", class: "close", data: { dismiss: "alert" } %>
    <%= msg %>
  </div>
<% end %>
